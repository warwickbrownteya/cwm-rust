# CONSTRUCT query - build new triples
PREFIX foaf: <http://xmlns.com/foaf/0.1/>
PREFIX ex: <http://example.org/>

CONSTRUCT {
    ?person ex:hasLabel ?name .
    ?person ex:ageInYears ?age .
}
WHERE {
    ?person a foaf:Person .
    ?person foaf:name ?name .
    ?person foaf:age ?age .
}
